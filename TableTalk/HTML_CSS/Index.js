import React form 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App form './App';
import reporyWebVisate from './reportWebVitals';
const root = ReactDom.createRoot (document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
  );
  reporyWebVisate();